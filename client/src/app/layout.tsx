import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "CryptoTracker - Real-time Cryptocurrency Dashboard",
  description: "Track top 10 cryptocurrencies with real-time prices, market cap, and 24h changes. Built with Next.js and CoinGecko API.",
  keywords: "cryptocurrency, crypto tracker, bitcoin, real-time prices, market cap",
  authors: [{ name: "VR Automations" }],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
